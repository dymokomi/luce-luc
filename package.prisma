#prisma 4.0
def package "luc" {
    str owner = "dymokomi"
    str version = "0.16.0"
    str kind = "tool"
    str language = "luce-base"
    str description = "The project tool for Luce and Luce Base: create, build, run and test projects, fetch dependencies, install applications and publish releases."
    str readme = "README.md"
    str entry = "src/luc/main.lucb"

    def dependency "luce-pkg" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-pkg"
    }

    def dependency "luce-crypto" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-crypto"
    }

    def dependency "luce-git" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-git"
    }

    def dependency "luce-http-client" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-http-client"
    }


    def dependency "luce-prism" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../luce-prism"
    }
}
